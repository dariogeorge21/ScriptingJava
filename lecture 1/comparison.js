console.log(null >= 0); //here when relational operators are used, the null is compared as a numerical value TRUE

console.log(null==undefined)// true 

console.log(null==0) // here it is not a relational operator therefore null is not treated as a 0 //false

console.log(undefined==0)// false

console.log(undefined>=0)// false

console.log(undefined<0)//false